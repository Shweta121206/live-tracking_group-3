import React from 'react';
import { Container, Typography } from '@mui/material';

const VoyageReplay = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>Voyage Replay</Typography>
      <Typography variant="body1">Historical voyage playback - Analyst+ only</Typography>
    </Container>
  );
};

export default VoyageReplay;
