import React from 'react';
import { Container, Typography } from '@mui/material';

const DashboardList = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>My Dashboards</Typography>
    </Container>
  );
};

export default DashboardList;
