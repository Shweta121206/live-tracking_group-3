import React from 'react';
import { Container, Typography } from '@mui/material';

const PortDetails = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>Port Details</Typography>
    </Container>
  );
};

export default PortDetails;
