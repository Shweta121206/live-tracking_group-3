import React from 'react';
import { Container, Typography } from '@mui/material';

const PortList = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>Ports</Typography>
    </Container>
  );
};

export default PortList;
