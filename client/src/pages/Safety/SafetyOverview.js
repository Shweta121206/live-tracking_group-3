import React from 'react';
import { Container, Typography } from '@mui/material';

const SafetyOverview = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>Safety Overview</Typography>
    </Container>
  );
};

export default SafetyOverview;
